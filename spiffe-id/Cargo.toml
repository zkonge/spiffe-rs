[package]
name = "spiffe-id"
version = "0.0.0"
edition.workspace = true

[dependencies]
serde = { workspace = true, default-features = false, optional = true }

[features]
default = []
serde = ["dep:serde"]
