[package]
name = "spiffe-id"
version = "0.0.0"
edition = "2021"

[dependencies]
thiserror = { version = "2.0.3", default-features = true }
serde = { version = "1.0.215", default-features = true, optional = true }

[features]
default = []
serde = ["dep:serde"]
